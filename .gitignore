# 依赖
node_modules/

# 运行时数据（用户本地文献库与设置，不应上传）
data/
uploads/

# 打包产物
dist/
*.exe

# 缓存与日志
.npm-cache/
*.log
test.pdf

# 开发期临时脚本与测试产物
# 临时脚本统一放工作区级 .pdfsandbox/，不要提交进仓库；
# 尤其不要用 *.test.* / *_test.* / *-test.* 命名放在仓库内——会被 node --test 自动发现，污染测试结果
.pdfsandbox/
_vision_ui_test.png
_vision_*_test_data/

# 系统与编辑器
.DS_Store
Thumbs.db
.idea/
.vscode/
